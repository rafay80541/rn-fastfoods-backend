// ============================================================
// VERCEL ENTRY POINT
// Vercel runs this file as a serverless function instead of a
// traditional always-on server. It imports the same Express app
// we already built — nothing about your routes/controllers changes.
// ============================================================

import app from '../server.js';

export default app;